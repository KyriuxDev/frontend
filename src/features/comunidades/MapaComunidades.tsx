// Mobile: no heatmap (el mapa de reporte individual ya existe en Map.tsx)
import { ComunidadResumen } from './comunidad.types';

interface Props {
  comunidades: ComunidadResumen[];
}

export default function MapaComunidades(_props: Props) {
  return null;
}